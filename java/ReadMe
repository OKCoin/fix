#使用需知
1、配置quickfix-client.properties
  SenderCompID 需要配置为唯一一个值，建议使用UUID，注意一个示例使用一个即可。
2、配置AccountUtil.java
      将申请apiKey 和 sercretKey 分别赋值给对应属性
3、OKClientApplication 配置
       在onLogon 方法中注册相应请求数据
4、运行OKClient Main 方法即时可以通信



【2016年2月26日下午15时更新】

	新增OKFIX合约支持DEMO

	！！注意！！

	如果需要获取FIX合约服务，请及时更新字典文件FIX4.4.XML（DEMO中已更新最新XML）

